{
  "scripts": {
    "build": "cd Agentic_AI_Recruitment_Manager-main && npm install && npm run build && cd .. && cp -r Agentic_AI_Recruitment_Manager-main/dist ."
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}